[package]
name = "iunit"
version = "0.1.0"
authors = ["Giles Cope <gilescope@gmail.com>"]

[dependencies]
# Shame this isn't a dev-dependency. You would think a compiler plugin could be just that.
trait_tests = { path="procmacro/test_trait_derive"}